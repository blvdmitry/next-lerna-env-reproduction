{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "dev": "lerna run dev"
  }
}
